[project]
name = "buildguard"
version = "0.1.0"
description = "BuildGuard"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
  "services/app-backend",
  "services/pipeline-backend",
  "packages/buildguard_common",
]
